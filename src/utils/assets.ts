const resourceBase = `${import.meta.env.BASE_URL}resources/`;

export const resources = {
  background: `${resourceBase}background.webp`,
  emptyState: `${resourceBase}alo-empty-state.png`,
  logoPixel: `${resourceBase}alo-logo-pixel.png`,
};
